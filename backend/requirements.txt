# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0      # ASGI server that runs FastAPI

# Database
sqlalchemy[asyncio]==2.0.36    # ORM (lets us use Python instead of SQL)
asyncpg==0.30.0                # Async PostgreSQL driver
alembic==1.14.0                # Database migration tool

# Validation & settings
pydantic[email]==2.10.0        # Data validation (used by FastAPI)
pydantic-settings==2.7.0       # Reads settings from .env files

# Authentication
python-jose[cryptography]==3.3.0   # JWT token encoding/decoding
passlib[bcrypt]==1.7.4             # Password hashing
bcrypt==4.0.1                      # Pin to version compatible with passlib

# Redis (for scaling WebSockets across multiple servers)
redis[asyncio]==5.2.0

# Rate limiting — caps how many requests one IP can make in a time window
slowapi==0.1.9

# Utilities
python-multipart==0.0.19       # Required for form data (file uploads later)
httpx==0.28.0                  # HTTP client for testing
